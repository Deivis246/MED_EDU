{
  "buildCommand": "cd ../.. && pnpm run build && cd artifacts/api-server && rm -rf dist-static && cp -r ../med-edu/dist dist-static",
  "outputDirectory": "dist-static",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.ts"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
